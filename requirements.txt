# Core transcription dependencies
# Core transcription model
openai-whisper>=20231117
# Core ML framework - use CPU version for memory efficiency
torch>=2.0.0
# Core ML framework - use CPU version for memory efficiency
torchaudio>=2.0.0

# Audio processing
# Audio processing - lightweight alternative to librosa
pydub>=0.25.1
# Numerical computing - essential for audio processing
numpy>=1.21.0
# Advanced audio processing - can be disabled for memory efficiency
librosa>=0.10.0
# Noise reduction - optional for memory efficiency
noisereduce>=3.0.0
webrtcvad>=2.0.10
scipy>=1.9.0

# Progress tracking and utilities
tqdm>=4.64.0

# Terminal display and Unicode support
colorama>=0.4.6
rich>=13.0.0
blessed>=1.20.0

# RTL (Right-to-Left) text support for Persian/Arabic
python-bidi>=0.4.2
arabic-reshaper>=3.0.0

# Advanced Audio Processing (Optional)
# Install with: pip install facebook-denoiser
# facebook-denoiser>=0.1.0
# denoiser>=0.1.1

# Performance monitoring and SSL
psutil>=5.9.0
certifi>=2023.0.0

# Optional: For better audio processing (if not using pydub)
# librosa>=0.10.0

# Development and debugging (optional)
# ipython>=8.0.0
# jupyter>=1.0.0

# Memory Optimization Tips:
# 1. Use --quality memory-optimized for low RAM systems
# 2. Consider using CPU-only torch: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
# 3. Disable optional dependencies for minimal memory usage
# 4. Use smaller Whisper models (tiny, base, small) for memory efficiency